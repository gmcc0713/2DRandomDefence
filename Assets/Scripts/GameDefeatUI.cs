using System.Collections;
using TMPro;
using UnityEngine;

public class GameDefeatUI : MonoBehaviour
{
    // Start is called before the first frame update
    [SerializeField] private TextMeshProUGUI[] gameOverText;

    private IEnumerator GameOverTextMove()
    {


        yield return null;


    }
}
